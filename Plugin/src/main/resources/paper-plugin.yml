name: "LibreLogin"
authors: [ "vuxeim", "sapphirecode", "Navio1430", "kyngs" ]
version: '${version}'
main: "xyz.kyngs.librelogin.paper.PaperBootstrap"
api-version: "1.19"

dependencies:
  server:
    Geyser-Spigot:
      load: "BEFORE"
      required: false
    floodgate:
      load: "BEFORE"
      required: false
    LuckPerms:
      load: "BEFORE"
      required: false
    ProtocolLib:
      load: "BEFORE"
      required: false
    ProtocolSupport:
      load: "BEFORE"
      required: false
    ViaVersion:
      load: "BEFORE"
      required: false
    ViaBackwards:
      load: "BEFORE"
      required: false
    ViaRewind:
      load: "BEFORE"
      required: false
